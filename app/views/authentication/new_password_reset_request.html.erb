<h1>Reset hesla</h1>

<div class="ui stackable grid">
  <div class="ui eight wide column">
    <div class="ui raised segment">
      <div class="ui form">
        <%= form_with url: reset_password_url, method: :post do %>
          <div class="field">
            <%= label_tag :username, "Školní xname" %>
            <div class="ui right labeled input">
              <%= text_field_tag :username, "", pattern: "^[a-z0-9]+$",
                                 placeholder: "Např. vrbj04",
                                 autofocus: true,
                                 required: true %>
              <div class="ui label">
                @vse.cz
              </div>
            </div>
          </div>

          <% if Rails.env.production? %>
            <div class="field">
              <%= label :user, :hcaptcha, "Ochrana proti robotům" %>
              <%= hcaptcha_tags class: "mt-2", theme: :light %>
            </div>
          <% end %>

          <%= submit_tag "Odeslat email s odkazem", class: "ui button primary" %>
        <% end %>
      </div>
    </div>
  </div>


  <div class="eight wide column">
    <div class="ui message">
      <b>Jak obnovit heslo?</b>
      <p>
        Ve formuláři vlevo vyplňte vaše xname a na školní email vám přijde email s odkazem, po jehož rozkliknutí bude
        možné nastavit si nové heslo.
      </p>
    </div>
  </div>
</div>
