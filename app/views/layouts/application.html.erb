<!DOCTYPE html>
<html>
<head>
  <title>Studentský portál FIS VŠE</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  <script src="https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js"></script>
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.7/dist/semantic.min.css">
  <script src="https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.7/dist/semantic.min.js"></script>
</head>

<body>
<div>
  <header class="mdc-top-app-bar mdc-top-app-bar--fixed-adjust">
      <a class="navbar-brand" href="<%= root_url %>">
        <h1 class="h3">Studentský portál</h1>
        <p class="text-uppercase text-muted fw-bold fs-6">Komunitní server pro studenty FIS VŠE v Praze</p>
      </a>
    <div class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <ul class="nav">
          <li class="nav-item">
            <%= link_to "Domovská stránka", root_url, class: "nav-link text-black-50" %>
          </li>
          <% if authenticated? %>
            <li class="nav-item">
              <%= link_to "Discord", discord_url, class: "nav-link text-black-50" %>
            </li>
          <% end %>
        </ul>
        <ul class="nav ms-auto">
          <% if authenticated? %>
            <% if current_user.is_admin %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-black-50" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Administrace
                </a>
                <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                  <%= link_to "Správa uživatelů", users_url, class: "dropdown-item text-dark" %>
                  <%= link_to "Správa API tokenů", api_tokens_url, class: "dropdown-item text-dark" %>
                  <%= link_to "Správa Discord serverů", discord_servers_url, class: "dropdown-item text-dark" %>
                  <%= link_to "Správa Předmětů", subjects_url, class: "dropdown-item text-dark" %>
                </ul>
              </li>
            <% end %>
            <li class="nav-item">
              <a href="<%= user_url(current_user) %>" class="nav-link <%= current_user.is_admin ? 'text-danger' : 'text-dark' %>">
                <img src="<%= current_user.avatar_url 32 %>" class="rounded-circle me-2">
                <%= current_user.display_name %>
              </a>
            </li>
            <li class="nav-item">
              <%= link_to "Odhlásit se", logout_url, class: "nav-link text-black-50" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Přihlášení", sign_in_url, class: "nav-link text-black-50" %>
            </li>
            <li class="nav-item">
              <%= link_to "Registrace", sign_up_url, class: "nav-link text-black-50" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </header>
  <main class="mdc-top-app-bar--fixed-adjust">

    <% unless flash[:alert].nil? %>
      <div class="alert alert-danger">
        <div class="container">
          <% if flash[:alert].respond_to?(:each) %>
            <% flash[:alert].each do |alert| %>
              <%= alert %><br>
            <% end %>
          <% else %>
            <%= flash[:alert] %>
          <% end %>
        </div>
      </div>
    <% end %>

    <% unless flash[:notice].nil? %>
      <div class="alert alert-info">
        <div class="container">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>

    <div class="container my-3 flex-grow-1">
      <%= yield %>
    </div>
  </main>

  <footer class="bg-dark py-3">
    <div class="container text-light">
      <div class="d-flex align-items-center justify-content-between">
        <div>
          <b>&copy; Jiří Vrba, Richard Kraus 2021</b><br>
          <a href="https://github.com/jirkavrba/studentsky-portal-fis-vse" target="_blank">Zdrojové kódy</a>
        </div>
      </div>
    </div>
  </footer>
</div>
</body>
</html>
